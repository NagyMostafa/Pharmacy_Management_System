/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Code;

import GUI.Login;
import java.io.File;

/**
 *
 * @author N A G Y
 */
public class SA1 {
    
    /*
      public static void main(String[] args) {

        String projectPath = System.getProperty("user.dir");
        System.out.println("Project Path: " + projectPath);
        File currentDir = new File(projectPath); // current directory				
     //   checkDirectoryContents(currentDir);

        
        new Login().setVisible(true);
    }
/*
    public static void checkDirectoryContents(File dir) {
        File[] files = dir.listFiles();
        boolean MedicineFile = true;
        boolean StoreFile = true;


        for (File file : files) {

        if (file.getName().contains("Medicine.bin")) {
            MedicineFile = false;
            
        if (file.getName().contains("Store.bin")) {
            StoreFile =false;
        }
        if (MedicineFile) {
            Medicine x = new Medicine();
            x.commitToFile();
        }
      if (StoreFile) {
            Store x = new Store();
            x.commitToFile();
        }
   }
}
}*/
}


 



